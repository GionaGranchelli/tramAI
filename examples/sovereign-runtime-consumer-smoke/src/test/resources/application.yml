tramai:
  profile: sovereign
  sovereign:
    enabled: true
    allowed-models:
      - local-invoice-model
    allowed-providers:
      - local-provider
    allowed-tools:
      - schedule-payment
    allowed-permissions:
      - payment.schedule
    provider-zones:
      local-provider: LOCAL
    models:
      local-invoice-model: local-provider
