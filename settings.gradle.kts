rootProject.name = "tramai"

include(
    "tramai-anthropic",
    "tramai-bom",
    "tramai-core",
    "tramai-engine",
    "tramai-observability",
    "tramai-orchestration",
    "tramai-openai",
    "tramai-ollama",
    "tramai-spring",
    "tramai-scheduler",
    "tramai-standalone",
    "tramai-structured",
    "tramai-testing",
)
