rootProject.name = "tramai"

include(
    "tramai-anthropic",
    "tramai-bom",
    "tramai-core",
    "tramai-engine",
    "tramai-observability",
    "tramai-mcp",
    "tramai-orchestration",
    "tramai-openai",
    "tramai-ollama",
    "tramai-spring",
    "tramai-scheduler",
    "tramai-server",
    "tramai-standalone",
    "tramai-structured",
    "tramai-testing",
)
