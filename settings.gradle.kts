rootProject.name = "tramai"

include(
    "tramai-anthropic",
    "tramai-bom",
    "tramai-core",
    "tramai-engine",
    "tramai-observability",
    "tramai-openai",
    "tramai-ollama",
    "tramai-spring",
    "tramai-standalone",
    "tramai-structured",
    "tramai-testing",
)
