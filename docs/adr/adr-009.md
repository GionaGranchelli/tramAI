# ADR-009: Keep Retry Orchestration in aurora-engine and Structured Analysis in aurora-structured

- Status: accepted
- Date: 2026-04-18

## Context

The boundary between `aurora-engine` and `aurora-structured` is one of the most sensitive seams in Aurora's design.

Aurora needs both:

- engine-level ownership of operation orchestration, retry policy, and terminal failure behavior
- structured-output ownership of schema generation, response extraction, deserialization, and validation assessment

If parsing rules, validation logic, or schema-specific decisions leak into `aurora-engine`, the module boundary becomes brittle and the structured pipeline stops being a replaceable subsystem. If retry policy leaks into `aurora-structured`, the engine loses control of execution semantics.

## Decision

Aurora will keep retry orchestration in `aurora-engine` and structured-output analysis in `aurora-structured`.

The boundary works as follows:

- `aurora-engine` owns retry policy, attempt counting, retry limits, and terminal exception behavior
- `aurora-structured` owns schema generation, raw response extraction, deserialization, and validation assessment
- `aurora-structured` returns a result object that describes structured-output success or failure and includes enough failure detail for the engine to decide the next step
- `aurora-engine` decides whether to continue, retry with feedback, or fail
- `aurora-engine` must not embed parsing or validation logic
- `aurora-structured` must not decide retry policy

## Consequences

Positive:

- preserves a clean module boundary
- keeps retry semantics consistent across engine execution paths
- allows structured-output behavior to evolve without spreading parsing logic through the engine

Negative:

- the result contract between modules must be designed carefully
- failure information needs to be rich enough for retries without overexposing structured internals

## Implementation Guidance

- Prefer a boundary type name broader than `ValidationResult` if it also represents parse and extraction failures.
- A name such as `StructuredOutputResult` is likely more accurate than a validation-only name.
- The engine should consume that result as an orchestration input, not as a parsing API surface.

## Related

- [ADR-003](./adr-003.md)
- [ADR-004](./adr-004.md)
- [DESIGN.md](/home/gionag/Development/aurora/DESIGN.md)
