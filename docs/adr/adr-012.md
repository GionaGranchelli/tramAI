# ADR-012: Keep `aurora-standalone` Minimal and Make Observability an Explicit Opt-In Module

- Status: accepted
- Date: 2026-04-18

## Context

Aurora wants two things at the same time:

- a strong observability story
- a minimal framework-free runtime with limited transitive dependency surface

Those goals are compatible only if automatic observability is understood as "auto-enable when the observability module is present" rather than "always bundled transitively by the standalone artifact."

If `aurora-standalone` pulls `aurora-observability` by default, then users who do not want OpenTelemetry dependencies still receive them transitively. That weakens the existing module philosophy that observability is opt-in, even if the runtime path is a no-op when OTel APIs are absent.

## Decision

Aurora will keep `aurora-standalone` minimal and make observability an explicit opt-in module.

The packaging model is:

- `aurora-standalone` composes the minimal non-framework runtime
- `aurora-observability` is a separate optional module
- observability auto-enables when the observability module and required OTel APIs are present
- observability is not a mandatory transitive dependency of the minimal standalone runtime

## Consequences

Positive:

- keeps the standalone artifact honest and lightweight
- preserves the claim that observability is optional at the dependency level
- reduces transitive dependency footprint for users who do not want OTel

Negative:

- users who want tracing must add one more artifact
- documentation must explain the difference between minimal standalone and standalone-plus-observability setups

## Implementation Guidance

- Define `aurora-standalone` as the composition of `aurora-core`, `aurora-engine`, and `aurora-structured`.
- Keep observability integration thin and attachable around the engine execution path.
- If Aurora later wants a batteries-included entry point, introduce a separate convenience artifact rather than making `aurora-standalone` heavier by default.

## Related

- [ADR-005](./adr-005.md)
- [ADR-006](./adr-006.md)
- [ADR-011](./adr-011.md)
- [DESIGN.md](/home/gionag/Development/aurora/DESIGN.md)
